server:
  port: 7001

eureka:
  instance:
    hostname: eureca7001.com   #eureca服务端的实例名称
  client:
    register-with-eureka: false #false表示不向注册中心注册自己
    fetch-registry: false #false表示自己端就是注册中心，职责时维护服务实例，并不需要去检索服务
   



